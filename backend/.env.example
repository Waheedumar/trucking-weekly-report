# Server
PORT=3002

# Auth
JWT_SECRET=change_me_in_production

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Claude API (used for reports, rate con parsing, and payroll vision)
ANTHROPIC_API_KEY=

# Resend (used for emailing payroll reports)
RESEND_API_KEY=
# Optional: verified sender. Defaults to onboarding@resend.dev if unset.
RESEND_FROM_EMAIL=Payroll <onboarding@resend.dev>
